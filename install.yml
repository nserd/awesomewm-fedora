---
- hosts: localhost
  connection: local

  roles:
    - role: awesome
      tags: [awesome]

    - role: kitty
      tags: [kitty]

    - role: picom
      tags: [picom]

  tasks:
    - name: Install additional packages
      dnf:
        name: "{{ item }}"
        state: present
      loop:
        - bash-completion
        - dbus-x11
        - flatpak
        - vim
        - htop
        - glibc-langpack-ru
        - flameshot
        - tar
        - tree
        - chromium
      become: true